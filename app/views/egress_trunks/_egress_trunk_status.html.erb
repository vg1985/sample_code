<% if egress_trunk.is_active? %>
  <%= link_to "Disable", deactivate_egress_trunk_path(egress_trunk), class: "btn btn-xs btn-warning", remote: true %>
<% else %>
  <%= link_to "Enable", activate_egress_trunk_path(egress_trunk), class: "btn btn-xs btn-success", remote: true %>
<% end %>
