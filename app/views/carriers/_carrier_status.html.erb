<% if carrier.is_active? %>
  <%= link_to "Disable", deactivate_carrier_path(carrier), class: "btn btn-xs btn-warning", remote: true %>
<% else %>
  <%= link_to "Enable", activate_carrier_path(carrier), class: "btn btn-xs btn-success", remote: true %>
<% end %>
