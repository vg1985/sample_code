App.overlayModal.hide();
<%if @response%>
	jQuery('#cdr_template_select').html('<%=escape_javascript(options_for_select(@cdr_templates.map {|t| [t.name, t.id]}, @template.id))%>').select2('val', '<%=@template.id%>');
<%end%>
