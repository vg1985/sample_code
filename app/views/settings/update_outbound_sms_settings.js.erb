var gritter_opts = {
	title: 'Outbound SMS Settings',
	sticky: false,
	time: '5000'
};

<%if @outbound_sms_setting.present? %>
	<%unless @outbound_sms_setting.errors.any?%>
		/*jQuery("span.bw_settings_form_msg").removeClass('text-muted text-danger').addClass('text-success').html('Bandwidth API settings updated successfully.');*/
		gritter_opts.text = "Outbound SMS settings have been updated.";
		gritter_opts.class_name = 'growl-success';
		
	<%else%>
		gritter_opts.text = "The data seems to be invalid. Failed to update the Outbound SMS settings.";
		gritter_opts.class_name = 'growl-danger';
	<%end%>
<%else%>
		gritter_opts.text = "This seems to be a invalid request. Aborting now.";
		gritter_opts.class_name = 'growl-danger';
<%end%>	

jQuery("span.sms_settings_form_msg").html('');
jQuery.gritter.add(gritter_opts);